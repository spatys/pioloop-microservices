version: '3.8'

services:
  property-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: property-api
    ports:
      - "5003:80"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_URLS=http://0.0.0.0:80
      - ConnectionStrings__DefaultConnection=Host=property-db;Database=pioloop_property;Username=pioloop12345;Password=piolOOP_&!?12345;Port=5432
      # Vercel Blob Storage
      - BLOB_READ_WRITE_TOKEN=vercel_blob_rw_9OrGtoLUaCxOp7Hw_gqEGs5dpYmx8xPoTHt9kTcMUX8xsPn
    depends_on:
      property-db:
        condition: service_healthy
    networks:
      - pioloop-network
    restart: unless-stopped

  property-db:
    image: postgres:15-alpine
    container_name: property-db
    environment:
      POSTGRES_DB: pioloop_property
      POSTGRES_USER: pioloop12345
      POSTGRES_PASSWORD: piolOOP_&!?12345
    ports:
      - "5435:5432"
    volumes:
      - property-db-data:/var/lib/postgresql/data
    networks:
      - pioloop-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U pioloop12345 -d pioloop_property"]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: unless-stopped

volumes:
  property-db-data:

networks:
  pioloop-network:
    external: true
